apply plugin: 'java'
apply plugin: 'findbugs'

version = '0.2.1'

subprojects {
    sourceCompatibility = '1.7'
    targetCompatibility = '1.7'

    repositories {
        mavenCentral()
    }

    findbugs {
        effort = 'max'
    }
}
