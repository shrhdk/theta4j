apply plugin: 'java'
apply plugin: 'findbugs'

version = '0.3.0'

subprojects {
    sourceCompatibility = '1.7'
    targetCompatibility = '1.7'

    repositories {
        mavenCentral()
    }

    findbugs {
        effort = 'max'
    }
}
