apply plugin: 'java'

version = '0.1.0'

task distribute(type: Zip, dependsOn: [clean, ':lib:distribute', ':cli:distribute', ':gui:distribute']) {
    from 'lib/build/libs'
    from 'cli/build/bin'
    from 'gui/build/bin'
}
